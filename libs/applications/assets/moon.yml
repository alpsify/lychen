id: applications-assets

language: assets

tasks:
  sync-covers:
    command:
      - ln
      - -sfn
      - $projectRoot/covers
    inputs:
      - '$projectRoot/covers/'
    options:
      cache: false

type: library
